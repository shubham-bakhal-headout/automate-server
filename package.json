{
  "name": "google-form-mock-server",
  "version": "1.0.0",
  "private": true,
  "description": "Mock server that returns a Google Forms autofill script for Chrome extension development.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
